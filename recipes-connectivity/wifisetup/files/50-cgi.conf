# Enable modules
server.modules += ( "mod_alias" )
server.modules += ( "mod_cgi" )

# Map /cgi-bin/ URL to /www/cgi-bin/ filesystem path
alias.url += ( "/cgi-bin/" => "/www/cgi-bin/" )

# Enable CGI for anything in /cgi-bin/
$HTTP["url"] =~ "^/cgi-bin/" {
    cgi.assign = ( "" => "" )
}
